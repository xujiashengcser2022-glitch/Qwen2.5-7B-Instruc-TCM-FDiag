Place the full released dataset file here:

  data/TCM-RAGF_processed_strict.json

Expected format: a JSON array of records, where each record contains:

- instruction
- input
- output
- filtered_symptom

The file is not duplicated in this generated package because the user indicated that the dataset has already been uploaded to GitHub.
